# Market Data Server Environment Variables

# ============================================
# Database Configuration (REQUIRED)
# ============================================
DATABASE_URL=postgresql://user:password@localhost:5432/market_data

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# Database Connection Pool (Optional)
# ============================================
# Maximum number of clients in the pool (default: 20)
DB_POOL_MAX=20

# How long a client is allowed to remain idle before being closed in ms (default: 30000)
DB_IDLE_TIMEOUT=30000

# Maximum time to wait for a connection in ms (default: 2000)
DB_CONNECT_TIMEOUT=2000

# ============================================
# SSL Configuration (Optional)
# ============================================
# Enable/disable SSL for database connection
# Set to 'false' to completely disable SSL (only in development!)
# DB_SSL=false

# Path to CA certificate file (optional, for production with self-signed certs)
# DB_CA_CERT=/path/to/ca-certificate.pem

# ============================================
# Broker API Credentials (Optional)
# ============================================
# OANDA API Configuration
OANDA_API_KEY=your_oanda_api_key_here
OANDA_ACCOUNT_ID=your_oanda_account_id_here

# Binance API credentials (if needed)
# BINANCE_API_KEY=your-api-key-here
# BINANCE_API_SECRET=your-api-secret-here

# ============================================
# Server Configuration (Optional)
# ============================================
# Server port (defaults to 8080 if not set)
# PORT=8080

# Allowed CORS origins (comma-separated list)
# Defaults to localhost:3000 and localhost:5173 if not set
# In production, set this to your actual frontend URLs
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend.com

# ============================================
# Notes
# ============================================
# - SSL is automatically enabled with proper verification in production (NODE_ENV=production)
# - In development, SSL allows self-signed certificates by default
# - Database pool settings have sensible defaults and are optional
# - Broker credentials are only needed if using those brokers